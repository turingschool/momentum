<%= form_with(model: job_report) do |form| %>
  <% if job_report.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(job_report.errors.count, "error") %> prohibited this job_report from being saved:</h2>

      <ul>
        <% job_report.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :sequence, style: "display: block" %>
    <%= form.select :sequence, options_for_select(named_enum(JobReport.sequences)) %>
  </div>

  <div>
    <%= form.label :company_id, style: "display: block" %>
    <%= form.select :company_id, options_for_select(Company.all.map{|c| [c.name, c.id]}) %>
  </div>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :salary, style: "display: block" %>
    <%= form.number_field :salary %>
  </div>

  <div>
    <%= form.label :start_date, style: "display: block" %>
    <%= form.date_field :start_date %>
  </div>

  <div>
    <%= form.label :end_date, style: "display: block" %>
    <%= form.date_field :end_date %>
  </div>

  <%= form.hidden_field :person_id %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
